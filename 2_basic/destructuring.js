const course={
    coursename:"niraj12234",
    courseteacher:"anish",
    fee:9990,
}
//  this is caleed destructuring of an object
const{courseteacher:name}=course
console.log(name);

// {} this eans destructuring
// JSON format 
// we write json format as {} as this .........in this the keys are also written as string and accesed as string 
